%{
local argArray = table.pack (...)
local group = argArray [1]
}
.. index:: pair: group; $(group.m_title)
.. _doxid-$(group.m_id):

$(getTitle (group.m_title, "="))

.. toctree::
	:hidden:

$(getCompoundTocTree (group, "\t"))

%{
includeFile ("c_compound.rst.in", group)
}
