%{
local argArray = table.pack (...)
local class = argArray [1]
local parentCompound = argArray [2]
}
.. _doxid-$(class.m_id):
.. index:: pair: $(class.m_compoundKind); $(getItemName (class, parentCompound))

$(getTitle (class.m_compoundKind .. " " .. getItemName (class, parentCompound), "="))

.. toctree::
	:hidden:

$(getCompoundTocTree (class, "\t"))

%{
includeFile ("c_compound.rst.in", class)
}
